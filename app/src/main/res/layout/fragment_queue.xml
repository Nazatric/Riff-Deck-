<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/riffdeck_chassis_bg"
    android:orientation="vertical">

    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- LCD-style panel label, echoing classic Winamp's "PLAYLIST EDITOR" title -->
    <com.google.android.material.textview.MaterialTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/riffdeck_lcd_bg"
        android:paddingHorizontal="8dp"
        android:paddingVertical="3dp"
        android:fontFamily="monospace"
        android:textStyle="bold"
        android:textColor="#4CFFB0"
        android:textSize="11sp"
        android:letterSpacing="0.08"
        android:text="@string/playing_queue_label"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/list_queue_padding_vertical">

        <include
            android:id="@+id/currentItem"
            layout="@layout/item_queue" />

    </FrameLayout>

    <com.google.android.material.divider.MaterialDivider
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/list_queue_padding_vertical"
        android:clipToPadding="false"
        android:overScrollMode="@integer/overScrollMode"
        android:scrollbars="none"
        tools:listitem="@layout/item_queue" />

</LinearLayout>